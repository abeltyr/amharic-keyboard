// Type definitions for the data objects

// Define the type for the words mapping object
export interface WordsMapping {
  [key: string]: string;
}

// Define the type for the numbers array
export type NumbersArray = string[];

// Define the types for the underHundred and overHundred objects
export interface NumberMapping {
  [key: number]: string;
}
